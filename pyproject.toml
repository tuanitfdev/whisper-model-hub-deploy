[project]
name = "whisper-model-hub-deploy"
version = "0.1.0"
description = "A project to deploy Whisper models to a model hub."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Your Name", email = "your.email@example.com" }]
dependencies = ["faster-whisper"]
